name: play_release
description: Upload a signed AAB to Google Play testing tracks.
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  args: ^2.6.0
  googleapis: ^14.0.0
  googleapis_auth: ^2.0.0
  http: ^1.2.0
  path: ^1.9.0

dev_dependencies:
  lints: ^5.1.1
  test: ^1.25.0
